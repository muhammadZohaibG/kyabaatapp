import 'package:flutter/material.dart';

class MeriKefiyatScreen extends StatelessWidget {
  const MeriKefiyatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

      ),
    );
  }
}
